#!/usr/bin/env python3

# Copyright 2020 Dirk Thomas
# Licensed under the Apache License, Version 2.0

"""
This script is only used when invoking from source.

The installed command is generated by the `console_scripts` entry point.
"""

import os
import sys

# add this package to the Python path
pkg_root = os.path.abspath(os.path.dirname(os.path.dirname(__file__)))
sys.path.insert(0, pkg_root)

from publish_python.cli import main  # noqa: E402


if __name__ == '__main__':
    try:
        rc = main()
    except RuntimeError as e:
        rc = str(e)
    sys.exit(rc)
